/// \file
/// Source for equivalence checking using the Birch-Dill methodology

#include "eq-check/verif_bd.h"

namespace ila {

#if 0
VerifBd::VerifBd(RefRelPtr ref) : ref_(ref) { ILA_NOT_NULL(ref); }

VerifBd::~VerifBd() {}

size_t VerifBd::unit_num() const { return ref_->unit_num(); }

bool VerifBd::CheckAll(const unsigned& bound) const {
  // TODO
  return true;
}

bool VerifBd::CheckUnit(const unsigned& bound, const size_t& i) const {
  // TODO
  return true;
}
#endif

} // namespace ila

